name: pintam2
description: PintaM² - app profesional para pintores.
publish_to: "none"
version: 0.8.0+8

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.13.0
  printing: ^5.15.0
  shared_preferences: ^2.5.3
  image_picker: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon.png
